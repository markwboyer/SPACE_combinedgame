using UnityEngine;

public class InitializeHazards : MonoBehaviour
{
    //public float maxX = 200;
    //public float maxY = 100;
    //public Rigidbody2D hazard;
    //public float maxScale = 1.0f;
    //public float minScale = 1.0f;
    // Start is called before the first frame update
    void Start()
    {
        //float xint = Random.Range(-maxX, maxX);
        //float yint = Random.Range(-maxY/3, maxY);
        //transform.position = new Vector3(xint, yint, 0);

        //float scale = Random.Range(minScale, maxScale);
        //transform.localScale = new Vector3(scale, scale, 1);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
