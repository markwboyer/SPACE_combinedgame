using UnityEngine;

public class BoulderPlacement : MonoBehaviour
{
    public Rigidbody2D Boulder1;
    public float maxX = 80;
    public float maxY = 40;

    // Start is called before the first frame update
    void Start()
    {

        //float xint = Random.Range(-maxX, maxX);
        //float yint = Random.Range(-maxY, maxY);
        //transform.position = new Vector3(xint, yint, 0);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
